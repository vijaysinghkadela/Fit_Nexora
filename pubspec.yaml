name: gymos_ai
description: "GymOS — AI-powered gym management SaaS platform"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  intl: ^0.19.0

  # Supabase
  supabase_flutter: ^2.8.4
  razorpay_flutter: ^1.3.7

  # State Management
  flutter_riverpod: ^2.6.1
  state_notifier: ^1.0.0

  # Routing
  go_router: ^14.8.1

  # Google font
  google_fonts: ^5.1.0

  # Environment Variables
  flutter_dotenv: ^5.2.1

  # Animations
  flutter_animate: ^4.5.2

  # Persistence
  shared_preferences: ^2.3.3

  # Utilities
  equatable: ^2.0.7
  json_annotation: ^4.9.0
  uuid: ^4.5.1
  cached_network_image: ^3.4.1
  shimmer: ^3.0.0
  flutter_svg: ^2.0.17
  url_launcher: ^6.3.1
  http: ^1.2.2
  fl_chart: ^0.66.0

  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.6

  # ── NEW FEATURES ──────────────────────────────────────────
  # Local push notifications (membership expiry, workout reminders)
  flutter_local_notifications: ^17.2.4
  # Timezone support for scheduled notifications
  timezone: ^0.9.4

  # Device connectivity detection (offline mode)
  connectivity_plus: ^6.1.1

  # Enhanced calendar widget for workout scheduling
  table_calendar: ^3.1.3

  # Circular/linear percentage progress indicators
  percent_indicator: ^4.2.3

  # Lottie animations (achievement unlock, empty states)
  lottie: ^3.1.2

  # Image picker (profile photo, progress photos)
  image_picker: ^1.1.2

  # Runtime permission requests (camera, notifications)
  permission_handler: ^11.3.1

  # Mobile barcode / QR scanner (food & gym check-in)
  mobile_scanner: ^5.2.3

  # Biometric authentication (fingerprint / Face ID)
  local_auth: ^2.3.0

  # Google Sign-In
  google_sign_in: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  json_serializable: ^6.9.4
  build_runner: ^2.4.14

flutter:
  generate: true
  uses-material-design: true

  assets:
    - .env
    - assets/images/
    - assets/icon/app_icon.png

# App Icon Configuration
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  web:
    generate: true
    image_path: "assets/icon/app_icon.png"
  image_path: "assets/icon/app_icon.png"
  adaptive_icon_background: "#030712"
  adaptive_icon_foreground: "assets/icon/app_icon.png"

# Splash Screen Configuration
flutter_native_splash:
  color: "#030712"
  image: assets/icon/app_icon.png
  image_dark: assets/icon/app_icon.png
  color_dark: "#030712"
  android: true
  ios: true
  web: true
  android_gravity: center
  ios_content_mode: center
  fullscreen: true










